[package]
name = "odid-difftest"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dev-dependencies]
odid = { path = "../odid", features = ["encode"] }

[build-dependencies]
cc = "1"
